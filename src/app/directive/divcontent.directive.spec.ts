import { DivcontentDirective } from './divcontent.directive';

describe('DivcontentDirective', () => {
  it('should create an instance', () => {
    const directive = new DivcontentDirective();
    expect(directive).toBeTruthy();
  });
});
